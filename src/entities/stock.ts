export interface IStockIndex {
  code: string;
  name: string;
  location: string;
  points?: number;
  variation: number;
}

export interface IStockIndex {
  name: string;
  location: string;
  points?: number;
  variation: number;
}
